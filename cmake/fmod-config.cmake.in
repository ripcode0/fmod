
add_library(fmod_lib SHARED IMPORTED GLOBAL)

set_target_properties(fmod_lib PROPERTIES
    IMPORTED_LOCATION "${CMAKE_CURRENT_SOURCE_DIR}/lib/${fmod_dll}"
    IMPORTED_IMPLIB "${CMAKE_CURRENT_SOURCE_DIR}/lib/${fmod_lib}"
    )
